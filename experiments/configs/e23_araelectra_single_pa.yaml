experiment_id: e23
model_name: aubmindlab/araelectra-base-discriminator
task_mode: single
subtasks: [PA]
epochs: 10
batch_size: 8
lr: 2.0e-5
max_length: 512
output_dir: outputs/e23
seed: 42
warmup_ratio: 0.1
weight_decay: 0.01
grad_clip: 1.0
num_workers: 4
trust_remote_code: false
pos_weight: 3.0   # AraELECTRA discriminator on PA — ELECTRA pretraining often strong on token labeling
