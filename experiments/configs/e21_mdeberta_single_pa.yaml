experiment_id: e21
model_name: microsoft/mdeberta-v3-base
task_mode: single
subtasks: [PA]
epochs: 10
batch_size: 8
lr: 2.0e-5
max_length: 512
output_dir: outputs/e21
seed: 42
warmup_ratio: 0.1
weight_decay: 0.01
grad_clip: 1.0
num_workers: 4
trust_remote_code: false
pos_weight: 3.0   # mDeBERTa-v3-base (multilingual) on PA — top single-model candidate vs XLM-R
